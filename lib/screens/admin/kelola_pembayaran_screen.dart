import 'package:flutter/material.dart';

class KelolaPembayaranScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Kelola Pembayaran'),
      ),
      body: Center(
        child: Text('Halaman untuk mengelola pembayaran'),
      ),
    );
  }
}
