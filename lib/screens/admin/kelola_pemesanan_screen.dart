import 'package:flutter/material.dart';

class KelolaPemesananScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Kelola Pemesanan'),
      ),
      body: Center(
        child: Text('Halaman untuk mengelola pemesanan'),
      ),
    );
  }
}
